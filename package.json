{
  "name": "acqua-almoxarifado",
  "version": "1.0.0",
  "private": true,
  "description": "Controle de almoxarifado com entrega de materiais e assinatura digital de Termo de Responsabilidade — Grupo Alcina Maria",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c yellow,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "start": "npm run start --workspace server",
    "seed": "npm run seed --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "trace:monogram": "node tools/trace-monogram.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
